# No third-party dependencies.
#
# All extractors are Python standard library only (Python 3.10+):
#   argparse, concurrent.futures, csv, glob, html, json, math, os, re,
#   statistics, time, urllib.
#
# The tests add only: socket, tempfile, unittest.
#
# Just run them with a system Python 3.10 or newer — nothing to pip install.
